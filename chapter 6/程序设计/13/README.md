# 题目
静态存储就是事先将存储数据元素所需的空间全部准备好。静态存储一般是将数据元素存储在一个数组中。动态存储是指在新添加一个数据元素时，为它申请一个动态变量。删除一个数据元素时，释放该元素的存储空间。顺序存储一般都用静态存储，而链接存储一般都用动态存储。但事实上，链接存储不一定要用动态存储，也可以用静态存储。哈夫曼树的实现就是一个很好的用静态存储的方式实现链接存储的实例。试修改代码清单6-20中的哈夫曼类，将哈夫曼树类改为用动态存储的方式实现。
> 代码清单6-20见haffman.c